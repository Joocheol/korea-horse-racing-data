# KRA 공급기관 정정 재검산 — 2026-08-27

## 결론

- 2025-10-17 제주·부경 API28_1/API29_1 공백은 행·경주·승식 범위 기준으로 복구됐다.
- 2019-05-18 제주 6경주 API26_2 `dusu`는 10으로 정정됐다.
- 2023-03-17 부경 API28_1 무효 자리표시자는 정정되지 않았다. 실제 경주 72행과
  존재하지 않는 9–12경주 128행, 총 200행이 현재 응답에도 남아 있다.

canonical 데이터는 자동 덮어쓰지 않았다. 2025년 HTML backfill은 새 API 응답과
key/value diff 0건을 확인한 뒤에만 API source로 교체한다.

## 실행 증거

| 항목 | 값 |
| --- | --- |
| GitHub Actions run | `33028591323` |
| artifact | `kra-provider-correction-audit` (`9629358212`) |
| artifact SHA-256 digest | `303aa350e5d497e64794232f5f2121cb28c0ed1e44fcab0a025b028314f8c6a0` |
| 확인 시각 | 2026-08-27 00:59 UTC |
| 표적 요청 | 11건 |

artifact는 11개 원문 JSON 응답, 요청별 SHA-256·행 수·완전성 판정, 요약을 포함한다.
모든 응답은 `totalCount == row_count`를 만족했다.

## 2025-10-17 공백

| 지역 | WIN·PLC | QNL | EXA | QPL | 합계 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 제주 | 158 | 351 | 702 | 351 | **1,562** |
| 부경 | 192 | 528 | 1,056 | 528 | **2,304** |
| 합계 | 350 | 879 | 1,758 | 879 | **3,866** |

각 응답은 1–8경주를 모두 포함한다. 합계 3,866행은 기존 HTML backfill의 제주
1,562행·부경 2,304행과 정확히 일치한다. 이는 endpoint 공백의 복구를 확인하는
강한 증거지만 숫자값 직접 동일성의 대체 증거는 아니다. 기존 authoritative API-only
bundle에는 이 다섯 승식 행이 없고, KRA HTML 화면은 재검산 환경에서 502로 열리지
않았으므로 이번 실행에서 3,866개 숫자값 diff는 수행하지 못했다.

## 2019-05-18 제주 6경주

API26_2 재조회에서 6경주는 10행, 고유 `chulNo` 10개(1–10), `dusu` 값 집합은
`[10]`이다. 과거 `dusu=9` 오류는 공급기관에서 정정됐다. 연구 파이프라인은
`registered_count`와 `starter_count`를 행·결과 상태로 재계산하므로 기존 분석값은
영향받지 않는다.

## 2023-03-17 부경

출전표 API26_2는 실제 1–8경주, 92두를 반환한다. API28_1 단승·연승은 1–12경주,
378행을 반환한다.

- 실제 1–8경주에서 출전표 밖 마번: 72행
- 존재하지 않는 9–12경주: 128행
- raw 응답의 무효행 합계: 200행

기존 canonical 규칙은 그대로 유효하다. 실제 경주의 72행은 출전표 참조 무결성으로
제외하고, 9–12경주 128행은 `race_record` universe 밖이어서 연구 odds에 포함하지
않는다.

## 반영 결정

1. 현재 HTML-backed canonical 29,196,005행은 유지한다.
2. 2025년 3,866행은 key/value 직접 대조가 완료될 때까지 source 교체하지 않는다.
3. 2019년 정정 상태를 문서화하되 계산 규칙은 바꾸지 않는다.
4. 2023년 예외 범위를 72행에서 raw 총 200행으로 확장해 기록하고 필터를 유지한다.
5. 재검산 도구는 원문을 자동 보존하지만 canonical을 자동 덮어쓰지 않는다.
