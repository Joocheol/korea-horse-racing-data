# KRA 공개 API 조사 결과 (2026-08-18)

공공데이터포털 한국마사회 OpenAPI를 실제 호출해 확인한 사실을 정리한 문서다.
수집기 설계와 검증 규칙의 근거로 삼는다.

모든 항목은 **실제 호출로 검증한 것**과 **추정**을 구분해 표기했다.
검증 없는 항목은 "미확인"으로 남겼다.

---

## 1. 엔드포인트

기준 URL: `https://apis.data.go.kr/B551015`

| 대상 | 서비스 | 오퍼레이션 | 상태 |
| --- | --- | --- | --- |
| 단승·연승 | `API28_1` | `singlePredictionRateInfo_1` | 검증 |
| 복승·쌍승·복연승 | `API29_1` | `doublePredictionRateInfo_1` | 검증 |
| 삼복승·삼쌍승 | `API30_1` | `triplePredictionRateInfo_1` | 검증 |
| 승식별 매출액 + 당첨배당 | `API179_1` | `salesAndDividendRate_1` | 검증 |
| 출전표 (등록두수 `dusu`) | `API26_2` | `entrySheet_2` | 검증 |
| 경주결과 상세 (착순·날씨·주로) | `API214_1` | `RaceDetailResult_1` | 검증 |
| 경주기록 | `API4_3` | `raceResult_3` | 검증 |
| 복승 (중복 출처, 교차검증용) | `API5` | `quinellaOddsInfo` | 검증 |
| 확정배당율 통합 | `API301` | `Dividend_rate_total` | 경로 미확인 |
| 경주일정 | `API72_2` | `racePlan_2` | 경로 미확인 |

### 공통 파라미터

```
serviceKey   인증키
pageNo       페이지 번호
numOfRows    페이지당 행수 (아래 3절 참고)
_type        json | xml
meet         1=서울 2=제주 3=부경 4=영천
rc_date      YYYYMMDD
rc_month     YYYYMM
rc_no        경주번호 (생략하면 해당 날짜/월 전체)
pool         승식 코드 (API29_1, API30_1에만 해당)
```

### 승식 코드

| 코드 | 승식 | 제공 API |
| --- | --- | --- |
| (없음) | 단승·연승 | `API28_1` (pool 파라미터 없음, 응답의 `pool` 필드로 구분) |
| `QNL` | 복승 | `API29_1` |
| `EXA` | 쌍승 | `API29_1` |
| `QPL` | 복연승 | `API29_1` |
| `TLA` | 삼복승 | `API30_1` |
| `TRI` | 삼쌍승 | `API30_1` |

---

## 2. 배당 격자는 전수다

각 API는 당첨 조합만이 아니라 **배당판 전체 조합**을 준다. 조합 수와 행 수가
정확히 일치함을 여러 경주에서 확인했다.

| 경주 | 출주두수 n | API | 행 수 | 이론값 |
| --- | ---: | --- | ---: | --- |
| 2025-03-08 서울 1R | 11 | `API28_1` | 22 | 단승 n=11 + 연승 n=11 |
| 2025-03-08 서울 1R | 11 | `API29_1` | 220 | 쌍승 n(n-1)=110 + 복승 C(n,2)=55 + 복연승 55 |
| 2025-03-08 서울 1R | 11 | `API30_1` TRI | 990 | n(n-1)(n-2)=990 |
| 2025-03-08 서울 1R | 11 | `API30_1` TLA | 165 | C(n,3)=165 |
| 2016-09-25 서울 2R | 10 | `API30_1` TRI | 720 | 720 |
| 2016-09-25 서울 2R | 10 | `API30_1` TLA | 120 | 120 |
| 2021-05-15 서울 1R | 12 | `API30_1` TRI | 1,320 | 1,320 |

11두 경주 한 건의 7승식 전체 = **1,397행**.

### 취소마 처리

API는 **출주두수 기준**으로 조합을 만든다. 등록두수가 아니다.

2025-03-08 서울 하루 복승 `totalCount = 520`.
- 출주두수 기준 `Σ C(출주두수, 2)` = 520 → **일치**
- 등록두수 기준 `Σ C(등록두수, 2)` = 550 → 불일치

### 표시상한 9999.9

**API에도 `9999.9` 상한이 그대로 있다.** 배당판 HTML과 동일한 인코딩이다.
HTML에서 상한이던 복승 셀 8건을 API로 조회한 결과 8건 모두 `9999.9`를 반환했다.
API로 검열 문제를 우회할 수 없다.

상한 셀 비율 예시:
- 2016-09-25 서울 2R 삼쌍승: 720셀 중 243셀
- 2025-03-08 서울 1R 삼쌍승: 990셀 중 477셀

---

## 3. `numOfRows` 상한

**문서의 3,000은 실제 상한이 아니다.** 100,000까지 정상 수용된다.

```
numOfRows=  10000  →  10,000행 수신 (totalCount 115,356)
numOfRows=  20000  →  20,000행 수신
numOfRows=  50000  →  50,000행 수신
numOfRows= 100000  → 100,000행 수신
```

`rc_no`를 생략하면 하루 전체, `rc_month`를 쓰면 한 달 전체가 온다.
서울 2025-03 삼쌍승 한 달 = 115,356행.

**따라서 경마장 × 월 단위 수집이 가능하다.** 전 역사를 약 3,700회 호출로
받을 수 있다(아래 6절).

---

## 4. 자료 제공 범위

각 API가 실제로 반환하는 최초 시점. 월 단위로 훑어 확인했다.

| 데이터 | 서울 | 제주 | 부경 | 영천 |
| --- | --- | --- | --- | --- |
| 매출액 | 1991-01 | 1993-04 | 2005-09 | 없음 |
| 단승·연승 | 2002-01 | 2002-01 | 2005-09 | 없음 |
| 복승·쌍승·복연승 | 2007-01 | 2007-01 | 2007-01 | 없음 |
| 삼복승 (TLA) | 2009-03 | 2009-04 | 2009-03 | 없음 |
| 삼쌍승 (TRI) | 2016-03* | 2016-03* | 2016-03* | 없음 |

\* 2016-03은 시험 데이터다. 5절 참고.

`meet=4`(영천)는 네 API 모두 자료가 없다. API 문서에는 영천 지원이 명시되어
있으나 실제 반환은 없다.

2007-01, 2002-01처럼 1월에 딱 맞춰 시작하는 것은 **승식 도입일이 아니라
자료 공개 경계**다. 복승·쌍승은 실제로는 훨씬 이전부터 시행됐다.

---

## 5. ⚠ API에 시험 데이터가 섞여 있다

**정규 경마일이 아닌 날짜의 기록이 API에 포함된다.** 그대로 수집하면 표본이
오염된다.

한국마사회는 금·토·일에만 경마를 시행한다. 그런데 삼쌍승 데이터를 요일별로
보면 다음이 나온다.

| 날짜 | 요일 | 서울 | 제주 | 부경 |
| --- | --- | ---: | ---: | ---: |
| 2016-03-15 | **화** | 5경주 | 3경주 | — |
| 2016-03-17 | **목** | — | — | 1경주 |
| 2016-06-29 | **수** | 5경주 | — | — |
| 2016-07-21 | **목** | 1경주 | 1경주 | 1경주 |

2016-07-21은 세 경마장이 나란히 1경주씩이다. 시스템 시험으로 보인다.

### 삼쌍승 실제 도입일

2016-03 시험 기록 이후 2016-04·05는 완전히 비어 있고,
**2016-06-10(금) 제주·부경, 2016-06-11(토) 서울**부터 정규 발매가 시작된다.
알려진 도입일 2016-06-10과 일치한다.

### 수집기 요구사항

1. **요일 필터** — 금·토·일이 아닌 개최일은 격리한다.
2. **격리한 날짜는 삭제하지 않고 별도 테이블에 보존**한다. 판단이 바뀔 수
   있고, 제외 근거를 증명할 수 있어야 한다.
3. 삼복승(2009-03)·복승(2007-01)·단승(2002-01)의 최초 기록도 같은 요일 검사를
   거쳐야 한다. 아직 확인하지 않았다.

---

## 6. 수집 규모 추정

경마장 × 월 단위, `numOfRows=100000` 기준.

| 대상 | 범위 | 경마장×월 | 예상 호출 |
| --- | --- | ---: | ---: |
| 삼복승·삼쌍승 | 2009~2026 | 약 640 | 약 900 |
| 복승·쌍승·복연승 | 2007~2026 | 약 720 | 약 750 |
| 단승·연승 | 2002~2026 | 약 870 | 약 870 |
| 매출액 | 1991~2026 | 약 1,200 | 약 1,200 |
| **합계** | | | **약 3,700** |

일일 트래픽 한도는 개발계정 기준 API별 10,000회다. 단
**`API179_1`(매출액)만 3,000회**로 낮다. 운영단계는 자동승인이 아니라
심의승인이다.

전 역사를 하루 안에 받을 수 있는 규모다.

---

## 7. 함정: 서비스키 인코딩

공공데이터포털은 같은 키를 두 형태로 준다.

- **Decoding** — 원본. `+` `/` `=` 포함
- **Encoding** — 이미 URL 인코딩됨. `%2B` `%2F` `%3D` 포함

Encoding 키에 `curl --data-urlencode`나 `requests`의 `params=`를 쓰면 `%`가
`%25`로 이중 인코딩되어 **`SERVICE_KEY_IS_NOT_REGISTERED_ERROR`가 뜬다.**
키가 등록되어 있어도 그렇게 보인다.

**권장: Decoding 키를 저장하고 라이브러리의 자동 URL 인코딩에 맡긴다.**

### 오류 코드 구분

| 오류 | 의미 |
| --- | --- |
| `NO_OPENAPI_SERVICE_ERROR` | **경로가 틀렸다.** 키와 무관하므로 키 없이도 경로 존재 확인에 쓸 수 있다 |
| `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` | 경로는 맞고, 키 미등록 또는 인코딩 문제 |
| `SERVICE_KEY_IS_NULL` | 키 파라미터가 전달되지 않았다 |

### 오퍼레이션 이름 규칙

서비스와 오퍼레이션 **양쪽에 `_1` 접미사**가 붙는 경우가 많다.
포털 화면에는 오퍼레이션만 보이고 서비스 경로는 잘려 있을 수 있으니,
**미리보기 버튼으로 열리는 실제 URL을 확인**하는 것이 가장 확실하다.

2018년판 활용가이드 문서의 경로는 이미 낡았다
(예: 문서의 `API30/triplePredictionRateInfo` → 실제 `API30_1/triplePredictionRateInfo_1`).

---

## 8. 매출액 API의 부가 정보

`API179_1/salesAndDividendRate_1`은 경주당 7행(승식별)을 준다.

```json
{"amt": 413419800, "meet": 1, "odds": "⑪③⑥-1077.1",
 "pool": "삼쌍", "rcDate": 20250308, "rcNo": 1}
```

- `amt` — 승식별 매출총액(취급액)
- `odds` — 당첨 조합과 확정배당 (원문자 마번 + 배당)
- `pool` — 단식 / 연식 / 복식 / 쌍식 / 복연 / 삼복 / 삼쌍

### 환급률 검산 통과

파리뮤추얼 항등식 `n = t·S / (100·O)`로 역산한 마권 수가 정수로 떨어진다.

| 승식 | 매출액 S | 배당 O | 환급률 t | 역산 마권 수 |
| --- | ---: | ---: | ---: | ---: |
| 삼쌍 | 413,419,800 | 1,077.1 | 0.73 | 2,802 |
| 단식 | 21,591,100 | 12.2 | 0.80 | 14,158 |

**API의 매출액이 배당판의 매출총액과 같은 정의임이 확인된다.**

### 연승 cutoff k를 직접 관측할 수 있다

`연식` 행에 지급된 연승 배당이 모두 실린다.

```
"odds": "⑪-2  ⑥-2.5  ③-3.5"    → k=3
```

`복연` 행도 마찬가지로 세 쌍이 실린다. 등록두수·출주두수만으로는 부분식별에
그치던 연승 지급 기준을 **관측값으로 확정**할 수 있다.

---

## 9. 기존 아카이브(v3) 대조에서 드러난 사실

2016-06~2025-12 구간을 HTML 스크레이핑으로 만든 기존 아카이브(19,301경주)와
API를 월 단위로 대조한 결과다.

2025년 36개(3경마장 × 12개월) 중 **8개월이 불일치했고, 8개 전부 API가 더
많았다.**

| 경마장·월 | 아카이브 | API | 차이 |
| --- | ---: | ---: | ---: |
| 서울 2025-03 | 5,413 | 6,133 | +720 |
| 부경 2025-08 | 3,099 | 3,727 | +628 |
| 부경 2025-03 | 3,901 | 4,502 | +601 |
| 서울 2025-10 | 2,919 | 3,496 | +577 |
| 부경 2025-10 | 2,424 | 2,771 | +347 |
| 제주 2025-05 | 3,011 | 3,210 | +199 |
| 제주 2025-12 | 2,400 | 2,494 | +94 |
| 부경 2025-12 | 3,327 | 3,382 | +55 |

일자별 추적 결과 **수집이 통째로 누락된 개최일이 존재**한다. 취소마 처리
차이가 아니다.

또 기존 아카이브에는 **2020·2021년이 통째로 없다**(수집 공백). API는 이 두 해를
정상 반환하므로 복구할 수 있다.

### 시사점

기존 파싱 검증(26개 항목)은 전부 "받아온 것끼리의 정합성"이었다. 애초에 받지
않은 경주는 원리적으로 검출할 수 없었다. 외부 기준선이 없었던 것이다.

**API를 상시 대조 기준선으로 두어야 한다.** 월별 셀 수 대조를 수집 완료
조건에 넣으면 같은 누락이 재발하지 않는다.

---

## 10. 검증 체크리스트

수집기가 통과해야 할 항목.

- [ ] 경주별 행 수 = 조합 수 (`n`, `n(n-1)`, `C(n,2)`, `C(n,3)`, `n(n-1)(n-2)`)
- [ ] 출주두수는 `API26_2/entrySheet_2`의 `dusu`와 취소마 정보로 독립 산출
- [ ] 복승식을 `API5`와 `API29_1` 양쪽에서 받아 셀 단위 대조
- [ ] 승식별 오버라운드 `Σ(1/O)`가 환급률과 정합
- [ ] 매출액 역산 마권 수가 정수
- [ ] 정규 경마일(금·토·일) 여부 검사, 비정규일 격리
- [ ] 월별 `totalCount`를 매니페스트에 기록해 재수집 시 대조
- [ ] 원응답 보존 및 체크섬

---

## 11. 미확인 항목

- `API301/Dividend_rate_total`(확정배당율 통합) 실제 경로
- `API72_2/racePlan_2`(경주일정) 실제 경로
- 삼복승(2009-03)·복승(2007-01)·단승(2002-01) 최초 기록의 요일 검사
- 영천(`meet=4`) 자료가 실제로 없는지, 아니면 다른 파라미터가 필요한지
- 2016~2024년 아카이브의 누락 개최일 전체 목록 (2025년만 확인)
- API 배당값과 배당판 HTML 값의 전수 일치 여부 (표본 8건만 확인)

---

*조사일 2026-08-18. 모든 수치는 실제 API 호출로 확인한 값이다.*
